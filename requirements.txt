# Runtime dependencies required by the proxy service (Playwright for browser
# login not included).
#
# 运行代理服务的必需依赖（不含浏览器登录所需的 Playwright）
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0
pydantic>=2.9,<3.0
