# Environment variables
# 环境变量
.env

# Sensitive data (captured cookies and tokens)
# 敏感数据 (抓取到的 Cookie 和 Token)
session.json
*.session.json

# Per-model probe cache (regenerable)
# 逐模型探测缓存（可再生）
model_probe_cache.json

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Playwright
playwright-browsers/

# Test artifacts
# 测试产物
tests/__pycache__/
tests/.tmp*/

# IDE
.vscode/
.idea/

# System
# 系统
.DS_Store
Thumbs.db

# Local workbench data (memory / config, not committed)
# 本地工作台数据（记忆 / 配置，不入库）
.workbuddy/
